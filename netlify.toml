[build]
  command = "npm run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback - all routes serve index.html (client-side routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
